{% extends 'back.html.twig' %}

{% block title %}Service
{% endblock %}

{% block body %}
	<h1>Service</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ service.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ service.name }}</td>
			</tr>
			<tr>
				<th>Price</th>
				<td>{{ service.price }}</td>
			</tr>
			<tr>
				<th>Duration</th>
				<td>{{ service.duration }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('dashboard_service_index') }}">back to list</a>

	<a href="{{ path('dashboard_service_edit', {'id': service.id}) }}">edit</a>

	{{ include('service/_delete_form.html.twig') }}
{% endblock %}
